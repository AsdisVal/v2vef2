<%- include('header') %>

<div class="container">
    <h1>Flokkar</h1>
    <ul>
        <% for(const category of categories.categories) {%>
            <li class="flokkar">
                <a href="/spurningar/<%= category.id %>"><%=category.name%></a>
            </li>
        <% } %>
    </ul>
</div>

<%- include('footer') %>