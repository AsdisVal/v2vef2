<% if (errors.length > 0) { %>
  <ul>
    <% errors.forEach(error => { %>
      <li><%= error %></li>
    <% }) %>
  </ul>
<% } %>

<form action="/question-form" method="POST">
  <label for="category">Veldu flokk:</label>
  <select name="category" id="category">
    <% categories.forEach(category => { %>
      <option value="<%= category.id %>"><%= category.nafn %></option>
    <% }) %>
  </select>
  
  <input type="submit" value="Búa til flokk">
</form>


<%- include('footer') -%>
